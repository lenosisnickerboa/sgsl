Update game functionality (same as install?)

Source code formatting

Restart program after install done. Consider adding a disabled restart button which is enabled when the install finishes successfully.

import subprocess
import sys

def restart_program():
    subprocess.Popen([sys.executable] + sys.argv)
    sys.exit(0)
